import { PageSkeleton } from "@/components/ui/PageSkeleton"

// Loading instan untuk area /admin (Server Component fetch data sistem).
export default function AdminLoading() {
  return <PageSkeleton />
}
